# Auto-generated tests for /mnt/data/maths.py
import pytest
import utils.maths

def test_func_get_derivative_callable():
    assert callable(getattr(utils.maths, 'get_derivative', None))

def test_func_trace_back_callable():
    assert callable(getattr(utils.maths, 'trace_back', None))

def test_func_compute_approximation_error_callable():
    assert callable(getattr(utils.maths, 'compute_approximation_error', None))

def test_func_constructor_callable():
    assert callable(getattr(utils.maths, 'constructor', None))

def test_func_get_slope_by_2_points_callable():
    assert callable(getattr(utils.maths, 'get_slope_by_2_points', None))

def test_func_max_fx_callable():
    assert callable(getattr(utils.maths, 'max_fx', None))

def test_func_min_fx_callable():
    assert callable(getattr(utils.maths, 'min_fx', None))

def test_func_df_callable():
    assert callable(getattr(utils.maths, 'df', None))

def test_func_second_df_callable():
    assert callable(getattr(utils.maths, 'second_df', None))
